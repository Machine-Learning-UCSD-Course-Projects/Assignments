M = 5;
N = 8;
%Input Sentence
x = [1 2 3 4 8];

%All the 8 tags
y = [1 2 3 4 5 6 7 8];

%Weight vector
w = ones(1,5)*0.1;

%Compute all gi matrices
g = computeG(x, y, w);

%Compute alpha matrix
alpha = computeAlpha(M,N,g)

%Compute beta matrix
beat = computeBeta(M,N,g)l



